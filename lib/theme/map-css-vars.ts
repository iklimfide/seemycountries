/** Map fill/stroke tokens — values come from CSS variables in globals.css */
export const MAP_CSS = {
  background: "var(--map-bg)",
  unvisited: "var(--map-unvisited)",
  oceanStroke: "var(--map-ocean-stroke)",
  pinCenter: "var(--map-pin-center)",
} as const;
